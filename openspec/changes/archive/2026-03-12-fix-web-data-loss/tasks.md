## 1. 修改 AuthProvider 恢复逻辑

- [x] 1.1 修改 `_initializeUser()` 方法，调整游客账号恢复优先级：先查询数据库中的游客账号，再检查设备标识匹配
- [x] 1.2 实现设备标识同步逻辑：当从数据库恢复游客账号时，将其 `device_id` 同步到 SharedPreferences

## 2. 处理多游客账号场景

- [x] 2.1 在查询游客账号时，按创建时间降序排序，选择最新的未删除游客账号

## 3. 测试验证

- [x] 3.1 验证首次启动创建游客账号正常工作（代码分析通过）
- [x] 3.2 验证 Web 平台上重复 `flutter run -d chrome` 能恢复原有游客账号
- [x] 3.3 验证设备标识同步逻辑正常工作