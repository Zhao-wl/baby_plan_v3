## 1. 修复月份显示问题

- [x] 1.1 添加 `_displayMonth` 状态变量，跟踪当前显示的月份
- [x] 1.2 修改 `_buildHeader()` 使用 `_displayMonth` 而非 `selectedDate` 计算月份
- [x] 1.3 在 `onPageChanged` 回调中更新 `_displayMonth`
- [x] 1.4 测试滑动切换周时月份正确更新

## 2. 添加展开/收起状态管理

- [x] 2.1 添加 `_isExpanded` 状态变量
- [x] 2.2 修改 `_buildHeader()` 点击事件为切换展开状态
- [x] 2.3 添加展开/收起图标指示（上下箭头）
- [x] 2.4 使用 AnimatedSize 包装内容区域实现高度动画

## 3. 实现月历视图组件

- [x] 3.1 创建 `MonthModel` 数据类，管理一个月的日期数据
- [x] 3.2 创建 `_buildMonthView()` 方法构建月历网格
- [x] 3.3 创建 `_buildMonthHeader()` 显示星期标题行
- [x] 3.4 创建 `_buildMonthDayCell()` 构建月历日期单元格
- [x] 3.5 实现月历 PageView，支持左右滑动切换月份
- [x] 3.6 添加月份切换箭头按钮（展开状态下显示）

## 4. 实现视图切换逻辑

- [x] 4.1 根据 `_isExpanded` 状态切换显示周视图或月视图
- [x] 4.2 展开状态下禁用周视图 PageController
- [x] 4.3 收起状态下禁用月视图 PageController
- [x] 4.4 展开状态下点击日期自动收起并选中
- [x] 4.5 同步周视图和月视图的当前月份状态

## 5. 优化交互细节

- [x] 5.1 添加展开/收起动画曲线（easeInOut）
- [x] 5.2 添加月份标题的点击反馈效果
- [x] 5.3 优化月历日期单元格样式（选中、今天、非当月日期）
- [x] 5.4 添加月历视图的左右滑动指示器

## 6. 测试与验证

- [x] 6.1 验证滑动周视图时月份标题正确更新
- [x] 6.2 验证点击月份标题展开月历视图
- [x] 6.3 验证月历视图左右滑动切换月份
- [x] 6.4 验证点击日期后正确选中并收起
- [x] 6.5 验证展开/收起动画流畅
- [x] 6.6 验证跨月滑动（如1月底到2月初）月份正确显示
- [x] 6.7 验证深色模式显示效果